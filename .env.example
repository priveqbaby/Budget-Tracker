# Leave unset to run in demo mode (in-memory seeded data, no auth).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-side only. Never exposed to the browser bundle.
ANTHROPIC_API_KEY=
